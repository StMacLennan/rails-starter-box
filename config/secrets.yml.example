# Add your own secrets here
# Rename this file secrets.yml
# Create a new key with `rails secret` in console

development:
  secret_key_base: yoursecretkey

test:
  secret_key_base: yoursecretkey

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
